name: Deploy Host App

on:
  push:
    branches: [main]
    paths: [ 'host-app/**' ]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-2
  ECR_REPOSITORY: host-app
  ECS_SERVICE: host-app-service
  ECS_CLUSTER: fiap-microfrontend
  REMOTE_ECS_SERVICE: remote-app-service

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::803872155909:role/GitHubActionsECRPushRole
        aws-region: ${{ env.AWS_REGION }}

    - uses: aws-actions/amazon-ecr-login@v2
      id: login-ecr

    - name: Discover Remote App Public IP (newest RUNNING task)
      run: |
        TASK_ARNS=$(aws ecs list-tasks \
          --cluster $ECS_CLUSTER \
          --service-name $REMOTE_ECS_SERVICE \
          --desired-status RUNNING \
          --query "taskArns[]" \
          --output text)

        if [ -z "$TASK_ARNS" ]; then
          echo "No RUNNING tasks found for $REMOTE_ECS_SERVICE"
          exit 1
        fi

        TASK_ARN=$(aws ecs describe-tasks \
          --cluster $ECS_CLUSTER \
          --tasks $TASK_ARNS \
          --query "sort_by(tasks,&startedAt)[-1].taskArn" \
          --output text)

        ENI_ID=$(aws ecs describe-tasks \
          --cluster $ECS_CLUSTER \
          --tasks $TASK_ARN \
          --query "tasks[0].attachments[0].details[?name=='networkInterfaceId'].value | [0]" \
          --output text)

        PUBLIC_IP=$(aws ec2 describe-network-interfaces \
          --network-interface-ids $ENI_ID \
          --query "NetworkInterfaces[0].Association.PublicIp" \
          --output text)

        if [ "$PUBLIC_IP" = "None" ] || [ -z "$PUBLIC_IP" ]; then
          echo "Remote task has no public IP (check Assign public IP and public subnet)."
          exit 1
        fi

        echo "NEXT_PUBLIC_REMOTE_APP_URL=http://$PUBLIC_IP:3000" >> $GITHUB_ENV
        echo "NEXT_PUBLIC_REMOTE_APP_URL=http://$PUBLIC_IP:3000"

    - name: Assert remoteEntry is reachable
      run: |
        echo "Using NEXT_PUBLIC_REMOTE_APP_URL=${{ env.NEXT_PUBLIC_REMOTE_APP_URL }}"
        curl -I "${{ env.NEXT_PUBLIC_REMOTE_APP_URL }}/_next/static/chunks/remoteEntry.js" | head -n 1

    - name: Build & Push
      working-directory: ./host-app
      run: |
        IMAGE=${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}
        docker build \
          --build-arg NEXT_PUBLIC_REMOTE_APP_URL=${{ env.NEXT_PUBLIC_REMOTE_APP_URL }} \
          -t $IMAGE .
        docker push $IMAGE


    - name: Create log group
      run: |
        aws logs create-log-group \
          --log-group-name /ecs/host-app \
          --region $AWS_REGION || true

    - name: Render task definition
      id: task-def
      uses: aws-actions/amazon-ecs-render-task-definition@v1
      with:
        task-definition: host-app/task-definition.json
        container-name: host-app
        image: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}
        environment-variables: |
          NEXT_PUBLIC_REMOTE_APP_URL=${{ env.NEXT_PUBLIC_REMOTE_APP_URL }}

    - name: Deploy ECS
      uses: aws-actions/amazon-ecs-deploy-task-definition@v1
      with:
        task-definition: ${{ steps.task-def.outputs.task-definition }}
        service: ${{ env.ECS_SERVICE }}
        cluster: ${{ env.ECS_CLUSTER }}
        wait-for-service-stability: true

    - name: Force desired count
      run: |
        aws ecs update-service \
          --cluster $ECS_CLUSTER \
          --service $ECS_SERVICE \
          --desired-count 1
